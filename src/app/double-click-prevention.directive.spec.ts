import { DoubleClickPreventionDirective } from './double-click-prevention.directive';

describe('DoubleClickPreventionDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleClickPreventionDirective();
    expect(directive).toBeTruthy();
  });
});
